# from math import sqrt
# list1 = [4, True, 'makers', 56, 25, 78, 9, False, 89.45, 100]

# new_list = list(map(lambda x: round(sqrt(x)), filter(lambda y: type(y) == int, list1)))
# print(new_list)


# dictionary = [{'name':'Alice', 'point':89},
#               {'name': 'John', 'point': 59},
#               {'name': 'Sam', 'point': 100},
#               {'name': 'Rachel', 'point': 45}]

# list1 = list(filter(lambda x: x['point'] < 60, dictionary))
# names = list(map(lambda x: x['name'], list1))
# points = list(map(lambda x: x['point'], list1))
# zipped_list = list(zip(names,points))

# expelled_list = list(map(lambda x: f'You are gonna be expelled {x[0]}', zipped_list))

# print(expelled_list)


# import functools
# letters = ['m', 'a', 'k', 'e', 'r', 's']
# str1 = functools.reduce(lambda x, y: x + y, letters)
# print(str1)


# name_of_list = ['Helloworld!']
# new_list  = list()
# for i in name_of_list:
    

